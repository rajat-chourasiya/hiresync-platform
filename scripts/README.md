# Scripts

Root-level seed, migration, and CI utility scripts.

## Structure
- `seed/` — Database seed scripts for local development and staging
- `migrate/` — MongoDB schema migration helpers
- `ci/` — CI/CD validation and environment check scripts
